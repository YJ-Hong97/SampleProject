<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="visitMapper">
<resultMap id="visit" type="com.sample.myapp.listner.VisitVo">
	<result column="visitDate" property="visitDate"/>
	<result column="visitCount" property="visitCount"/>
</resultMap>
<select id="selectVisit" resultMap="visit">
	select * from visit where visitDate=SYSDATE
</select>
<insert id="insertVisit">
	insert into visit values(
		SYSDATE,
		1
	)
</insert>
<update id="updateVisit">
	update visit set visitCount=(select visitCount from visit where visitdate=sysdate)+1 where visitdat=sysdate 
</update>
</mapper>